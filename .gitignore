# Python cache and build artifacts
__pycache__/
*.py[cod]
*.so
*.egg-info/
dist/
build/

# Virtual environments
.venv/
venv/
env/
.env/
.conda/

# IDE/editor settings
.vscode/
.idea/

# OS-specific noise
.DS_Store
Thumbs.db

# Secrets and runtime config
*.env

# App-specific data directories
data/chroma_db/
data/pdfs/*.pdf
data/pdfs/*.PDF
data/*
# But allow DVC tracking files (must come after data/ pattern)
!data/*.dvc
!data/**/*.dvc
# Allow .gitkeep files if needed
!data/**/.gitkeep

# Cache directories (HuggingFace models, etc.)
cache/
.cache/
*.cache


# DVC cache and internal files
.dvc/cache/
.dvc/tmp/
.dvc/config.local

# DVC outputs (if tracked by DVC, ignore the actual files)
data/chunks.json
# Note: data/chroma_db/ is already ignored (good!)
# Note: data/pdfs/ is already ignored (good if using DVC to track it)

# DVC metrics (optional - you can track these or ignore them)
metrics/


# SSH key
*.pem
ec2_deploy_key
ec2_deploy_key.pub
